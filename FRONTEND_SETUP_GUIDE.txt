"""
═══════════════════════════════════════════════════════════════════════════════
        🫁 FRONTEND SETUP GUIDE - REACT + TAILWIND CSS
═══════════════════════════════════════════════════════════════════════════════

COMPLETE STEP-BY-STEP GUIDE FOR SETTING UP THE REACT FRONTEND
"""

═══════════════════════════════════════════════════════════════════════════════
📋 PREREQUISITES
═══════════════════════════════════════════════════════════════════════════════

Before starting, make sure you have:
✅ Node.js (LTS version): https://nodejs.org/
✅ npm (comes with Node.js)
✅ Text Editor (VS Code recommended): https://code.visualstudio.com/
✅ Git (optional): https://git-scm.com/

Verify installation:
  node --version      # Should show v18+ 
  npm --version       # Should show 8+


═══════════════════════════════════════════════════════════════════════════════
🚀 QUICK START (5 MINUTES)
═══════════════════════════════════════════════════════════════════════════════

FOR WINDOWS:
  1. Double-click: SETUP.bat
  2. Wait for completion
  3. Open 2 terminals:
     Terminal 1: cd Backend && python app.py
     Terminal 2: npm start
  4. Open http://localhost:3000

FOR MAC/LINUX:
  1. chmod +x FRONTEND_SETUP.sh
  2. ./FRONTEND_SETUP.sh
  3. npm install
  4. Follow manual setup steps below


═══════════════════════════════════════════════════════════════════════════════
📦 MANUAL STEP-BY-STEP SETUP
═══════════════════════════════════════════════════════════════════════════════

STEP 1: CREATE REACT APP
────────────────────────────────────────────────────────────────────────────────

Option A - Using create-react-app (Recommended for beginners):
  
  npx create-react-app breathing-asthma-ai
  cd breathing-asthma-ai

Option B - Using Vite (Faster, modern):

  npm create vite@latest breathing-asthma-ai -- --template react
  cd breathing-asthma-ai
  npm install


STEP 2: INSTALL TAILWIND CSS
────────────────────────────────────────────────────────────────────────────────

Install Tailwind CSS and dependencies:

  npm install -D tailwindcss postcss autoprefixer

Initialize Tailwind configuration:

  npx tailwindcss init -p

This creates:
  • tailwind.config.js
  • postcss.config.js


STEP 3: CONFIGURE TAILWIND
────────────────────────────────────────────────────────────────────────────────

Edit tailwind.config.js:

module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}


STEP 4: ADD TAILWIND DIRECTIVES
────────────────────────────────────────────────────────────────────────────────

Edit src/index.css (or App.css):

@tailwind base;
@tailwind components;
@tailwind utilities;


STEP 5: COPY APP FILES
────────────────────────────────────────────────────────────────────────────────

Replace src/App.js with the provided App.js content:
  • Copy from: Breathing-Sound-Analysis-for-Asthma-Detection/src/App.js
  • Paste to: your-react-app/src/App.js

Replace src/App.css with the provided App.css content:
  • Copy from: Breathing-Sound-Analysis-for-Asthma-Detection/src/App.css
  • Paste to: your-react-app/src/App.css


STEP 6: START THE APP
────────────────────────────────────────────────────────────────────────────────

Run development server:

  npm start

This will:
  ✅ Start React dev server on http://localhost:3000
  ✅ Automatically open browser
  ✅ Hot reload on file changes


STEP 7: BACKEND API
────────────────────────────────────────────────────────────────────────────────

In another terminal, start Flask backend:

  cd Backend
  pip install -r requirements_full.txt
  python app.py

Backend will run on: http://localhost:5000


═══════════════════════════════════════════════════════════════════════════════
🎨 UI FEATURES & COMPONENTS
═══════════════════════════════════════════════════════════════════════════════

The React app includes:

✅ RESPONSIVE DESIGN:
   • Mobile-friendly (works on phone, tablet, desktop)
   • Gradient backgrounds (blue to indigo)
   • Medical color theme

✅ MULTI-STEP FORM:
   • Step 1: Personal Information (Name, Age, Gender, Weight, Height)
   • Step 2: Medical History (Symptoms, Duration, Family History, Smoking)
   • Step 3: Environmental Factors (Location, AQI, Profession, Allergies)
   • Step 4: X-ray Upload (Optional chest X-ray image)
   • Step 5: Results & Diagnosis (AI Analysis results)

✅ PROGRESS TRACKING:
   • Visual progress bar
   • Step indicators
   • Navigation buttons (Back/Next)

✅ RESULT DISPLAY:
   • Spirometry analysis results
   • X-ray analysis results (if uploaded)
   • Confidence scores
   • Risk level indicators
   • Medical recommendations

✅ ERROR HANDLING:
   • Form validation
   • API error messages
   • User-friendly alerts


═══════════════════════════════════════════════════════════════════════════════
🔗 API INTEGRATION
═══════════════════════════════════════════════════════════════════════════════

The React app connects to Flask backend at:
  http://localhost:5000

Two main endpoints used:

1. POST /predict
   Purpose: Spirometry severity prediction
   Sends: Patient symptoms, age, gender
   Returns: Severity level, confidence score

2. POST /analyze-xray
   Purpose: X-ray image analysis
   Sends: Chest X-ray image file
   Returns: Diagnosis, risk level, confidence


═══════════════════════════════════════════════════════════════════════════════
📁 PROJECT STRUCTURE
═══════════════════════════════════════════════════════════════════════════════

breathing-asthma-ai/
├── src/
│   ├── App.js                    # Main app component
│   ├── App.css                   # Styles + Tailwind
│   ├── index.js
│   └── index.css
├── public/
│   ├── index.html
│   └── favicon.ico
├── tailwind.config.js            # Tailwind configuration
├── postcss.config.js             # PostCSS configuration
├── package.json
└── node_modules/                 # Dependencies (auto-created)


═══════════════════════════════════════════════════════════════════════════════
🎨 TAILWIND CSS CUSTOMIZATION
═══════════════════════════════════════════════════════════════════════════════

Current color scheme (Medical Blue & White):
  • Primary: Blue (#2563eb)
  • Secondary: Indigo (#4f46e5)
  • Background: Light Blue (#f0f9ff)
  • Text: Dark Blue (#1e3a8a)
  • Success: Green (#16a34a)
  • Warning: Yellow (#eab308)
  • Error: Red (#dc2626)

To customize colors, edit tailwind.config.js theme section.


═══════════════════════════════════════════════════════════════════════════════
🔧 COMMON COMMANDS
═══════════════════════════════════════════════════════════════════════════════

Development:
  npm start              # Start dev server (port 3000)
  npm run build          # Create production build
  npm test               # Run tests
  npm eject              # Eject from create-react-app (⚠️ irreversible)

Dependencies:
  npm install            # Install dependencies
  npm install [package]  # Install specific package
  npm uninstall [package]# Remove package
  npm update             # Update all packages


═══════════════════════════════════════════════════════════════════════════════
🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

Issue: "Port 3000 already in use"
  Solution: 
    npm start -- --port 3001
    Or kill process: lsof -ti:3000 | xargs kill -9

Issue: "Cannot GET /predict"
  Solution:
    • Make sure backend is running (python app.py)
    • Check backend is on http://localhost:5000
    • Check CORS is enabled in Flask

Issue: "Tailwind styles not applying"
  Solution:
    • Clear npm cache: npm cache clean --force
    • Delete node_modules: rm -rf node_modules
    • Reinstall: npm install
    • Check content paths in tailwind.config.js

Issue: "React not loading"
  Solution:
    • Check browser console for errors
    • Verify App.js syntax
    • Clear browser cache (Ctrl+Shift+Delete)

Issue: "API calls timing out"
  Solution:
    • Verify backend is running
    • Check firewall settings
    • Try localhost instead of 127.0.0.1


═══════════════════════════════════════════════════════════════════════════════
📱 RESPONSIVE DESIGN TIPS
═══════════════════════════════════════════════════════════════════════════════

The app uses Tailwind CSS breakpoints:
  • sm: 640px
  • md: 768px
  • lg: 1024px
  • xl: 1280px
  • 2xl: 1536px

Classes automatically adjust layout for different screen sizes.

Test on mobile:
  npm start
  Then on mobile device, connect to: http://<your-ip>:3000


═══════════════════════════════════════════════════════════════════════════════
🚀 DEPLOYMENT
═══════════════════════════════════════════════════════════════════════════════

Build for production:
  npm run build

This creates optimized build in 'build/' folder.

Deploy to:
  • Vercel: vercel . (recommended for React)
  • Netlify: netlify deploy --prod --dir=build
  • GitHub Pages: npm run build && npm install -g gh-pages
  • AWS: aws s3 sync build/ s3://bucket-name


═══════════════════════════════════════════════════════════════════════════════
📚 FURTHER LEARNING
═══════════════════════════════════════════════════════════════════════════════

React Documentation:
  https://react.dev

Tailwind CSS Documentation:
  https://tailwindcss.com/docs

Create React App Documentation:
  https://create-react-app.dev


═══════════════════════════════════════════════════════════════════════════════
✅ VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

Frontend Setup:
  ✅ Node.js and npm installed
  ✅ React app created
  ✅ Tailwind CSS installed
  ✅ App.js and App.css copied
  ✅ npm start runs without errors
  ✅ UI displays in browser (localhost:3000)
  ✅ Medical color theme visible (blue & white)
  ✅ All 5 steps of form visible
  ✅ Form validation working

Backend Setup:
  ✅ Python 3.8+ installed
  ✅ Virtual environment created
  ✅ Dependencies installed
  ✅ Backend runs on localhost:5000
  ✅ CORS enabled
  ✅ API endpoints responding

Integration:
  ✅ React app connects to Flask
  ✅ Form data sends to backend
  ✅ Results display correctly
  ✅ No console errors


═══════════════════════════════════════════════════════════════════════════════
🎉 YOU'RE READY!
═══════════════════════════════════════════════════════════════════════════════

Now you have a professional medical AI diagnostic app!

Next steps:
  1. Start Backend: cd Backend && python app.py
  2. Start Frontend: npm start
  3. Open http://localhost:3000
  4. Test the full workflow
  5. Customize as needed
  6. Deploy when ready!


═══════════════════════════════════════════════════════════════════════════════

Questions? See: DOCUMENTATION_INDEX.md
"""
